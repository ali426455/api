# استقرار رایگان روی Streamlit Cloud

Koyeb را کنار بگذارید. برای این ربات، Streamlit Cloud ساده‌تر و پایدارتر است.

## مراحل (حدود ۲ دقیقه)

1. بروید به [https://share.streamlit.io](https://share.streamlit.io)
2. با **GitHub** وارد شوید (`ali426455`)
3. **Create app** را بزنید
4. این‌ها را انتخاب کنید:
   - Repository: `ali426455/api`
   - Branch: `arena/01a01b19-api`
   - Main file path: `app.py`
5. **Deploy** را بزنید

لینک عمومی چیزی شبیه این می‌شود:

`https://ali426455-api.streamlit.app`

## نکته

اگر ریپو را در لیست ندیدید، دسترسی GitHub را به ریپوی `api` بدهید.

هر بار که کد را push کنید، سایت خودش به‌روز می‌شود.
